---
phase: 03-template-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/create-aws-starter-kit/templates/**
autonomous: true
---

<objective>
Create template files for root configuration and shared packages with placeholder tokens.

Purpose: Convert the existing root config files and shared packages (common-types, api-client) into templates that can be copied and customized for new projects.
Output: Template files in packages/create-aws-starter-kit/templates/ with {{TOKEN}} placeholders
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-system/03-01-SUMMARY.md

@packages/create-aws-starter-kit/src/templates/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root configuration templates</name>
  <files>packages/create-aws-starter-kit/templates/root/</files>
  <action>
Create templates/root/ directory and copy the following root files, replacing "aws-starter-kit" references with {{PROJECT_NAME}} and "@aws-starter-kit" with {{PACKAGE_SCOPE}}:

Files to template (copy from project root):
- package.json → Replace name, description. Keep structure.
- tsconfig.base.json → Replace @aws-starter-kit paths with {{PACKAGE_SCOPE}}
- nx.json → Keep as-is (no project-specific values)
- jest.preset.js → Keep as-is
- eslint.config.js → Keep as-is
- .npmrc → Keep as-is
- .nvmrc → Keep as-is
- .editorconfig → Keep as-is
- .gitignore → Keep as-is

For package.json specifically:
- "name": "{{PROJECT_NAME}}"
- Remove workspaces entry for create-aws-starter-kit (the CLI package shouldn't be in generated projects)
- Keep all dependencies and scripts

For tsconfig.base.json:
- Replace "@aws-starter-kit/*" paths with "{{PACKAGE_SCOPE}}/*"

Do NOT include:
- package-lock.json (will be generated on npm install)
- .planning/ directory (planning docs)
- agents/ directory (Claude Code agent definitions)
- CLAUDE.md (Claude Code instructions)
- Any documentation files (README.md, QUICKSTART.md, etc. - will be generated fresh)
  </action>
  <verify>ls packages/create-aws-starter-kit/templates/root/ shows all config files</verify>
  <done>Root config templates created with proper token placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Create shared packages templates</name>
  <files>packages/create-aws-starter-kit/templates/packages/</files>
  <action>
Create templates/packages/ directory structure:
- templates/packages/common-types/
- templates/packages/api-client/

Copy all files from packages/common-types/ and packages/api-client/ with token replacements:

For package.json files:
- "name": "{{PACKAGE_SCOPE}}/common-types" and "{{PACKAGE_SCOPE}}/api-client"

For .eslintrc.json files:
- Keep as-is (no project-specific references)

For TypeScript source files (.ts):
- Replace '@aws-starter-kit/common-types' imports with '{{PACKAGE_SCOPE}}/common-types'

For project.json files:
- These use relative paths, keep as-is

Do NOT copy:
- node_modules/
- dist/
- Any generated files

Preserve directory structure exactly:
```
templates/packages/
├── common-types/
│   ├── package.json
│   ├── project.json
│   ├── tsconfig.json
│   ├── tsconfig.lib.json
│   ├── .eslintrc.json
│   └── src/
│       ├── index.ts
│       ├── api.types.ts
│       ├── common.types.ts
│       ├── lambda.types.ts
│       └── user.types.ts
└── api-client/
    ├── package.json
    ├── project.json
    ├── tsconfig.json
    ├── tsconfig.lib.json
    ├── tsconfig.spec.json
    ├── .eslintrc.json
    ├── jest.config.ts
    └── src/
        ├── index.ts
        ├── api-client.ts
        ├── config.ts
        └── __tests__/
            └── api-client.spec.ts
```
  </action>
  <verify>find packages/create-aws-starter-kit/templates/packages -name "*.ts" | head -10 shows TypeScript files</verify>
  <done>Shared package templates created with {{PACKAGE_SCOPE}} placeholders in imports</done>
</task>

<task type="auto">
  <name>Task 3: Create tooling templates (.vscode, .github)</name>
  <files>packages/create-aws-starter-kit/templates/.vscode/, packages/create-aws-starter-kit/templates/.github/</files>
  <action>
Create templates for IDE and CI/CD configuration:

templates/.vscode/:
- Copy .vscode/settings.json → Keep as-is
- Copy .vscode/extensions.json → Keep as-is

templates/.github/:
- Copy .github/actions/ directory → Keep as-is (no project-specific values)
- Copy .github/workflows/ directory → Replace any aws-starter-kit references:
  - Stack names like "AwsStarterKit-*" → "{{PROJECT_NAME_PASCAL}}-*"
  - Any hardcoded project names

For workflow files, scan for:
- Hardcoded stack names in deploy commands
- Environment variable references that include project name
- Any AWS resource names containing aws-starter-kit

Most workflow files should be kept as-is since they use generic patterns.
  </action>
  <verify>ls -la packages/create-aws-starter-kit/templates/.vscode/ && ls -la packages/create-aws-starter-kit/templates/.github/</verify>
  <done>IDE and CI/CD templates created with CDK stack name placeholders</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `nx build create-aws-starter-kit` succeeds (templates are just files, no compilation)
- [ ] templates/root/ contains all root config files
- [ ] templates/packages/common-types/ has all source files
- [ ] templates/packages/api-client/ has all source files
- [ ] templates/.vscode/ and templates/.github/ exist with contents
- [ ] grep "{{PROJECT_NAME}}" templates/root/package.json returns matches
- [ ] grep "{{PACKAGE_SCOPE}}" templates/packages/*/package.json returns matches
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Shared templates ready for any generated project
- No project-specific hardcoded values remaining in templates
- Foundation ready for Plan 03-03 to add platform-specific templates
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-system/03-02-SUMMARY.md`
</output>
