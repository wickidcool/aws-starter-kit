---
phase: 03-template-system
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - packages/create-aws-starter-kit/templates/apps/**
  - packages/create-aws-starter-kit/templates/manifest.json
autonomous: true
---

<objective>
Create platform-specific templates (web, mobile, api) and the template manifest for conditional file inclusion.

Purpose: Convert apps/web, apps/mobile, and apps/api into templates with platform markers, enabling the CLI to generate projects with only the platforms the user selected.
Output: Platform templates in packages/create-aws-starter-kit/templates/apps/ and manifest.json defining file-to-platform mappings
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-system/03-02-SUMMARY.md

@packages/create-aws-starter-kit/src/templates/tokens.ts
@packages/create-aws-starter-kit/src/templates/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create web app templates</name>
  <files>packages/create-aws-starter-kit/templates/apps/web/</files>
  <action>
Create templates/apps/web/ by copying apps/web/ with token replacements:

Token replacements needed:
- @aws-starter-kit/common-types → {{PACKAGE_SCOPE}}/common-types
- @aws-starter-kit/api-client → {{PACKAGE_SCOPE}}/api-client
- Any "aws-starter-kit" display text → {{PROJECT_NAME_TITLE}}

Files to copy (preserving directory structure):
```
templates/apps/web/
├── package.json (NOT needed - web doesn't have its own package.json)
├── project.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.spec.json
├── .eslintrc.json
├── .env.example
├── vite.config.ts
├── jest.config.ts
├── public/
│   └── (any public assets)
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── vite-env.d.ts
    ├── test-setup.ts
    ├── config/
    │   └── api.ts
    ├── store/
    │   └── user-store.ts
    ├── theme/
    │   └── index.ts
    ├── __tests__/
    │   ├── App.spec.tsx
    │   └── store/
    │       └── user-store.spec.ts
    └── __mocks__/
        └── config/
            └── api.ts
```

Do NOT copy:
- node_modules/
- dist/
- Any .env file (only .env.example)
  </action>
  <verify>find packages/create-aws-starter-kit/templates/apps/web -name "*.tsx" | wc -l shows at least 2 files</verify>
  <done>Web app templates created with {{PACKAGE_SCOPE}} import replacements</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile app templates</name>
  <files>packages/create-aws-starter-kit/templates/apps/mobile/</files>
  <action>
Create templates/apps/mobile/ by copying apps/mobile/ with token replacements:

Token replacements needed:
- @aws-starter-kit/common-types → {{PACKAGE_SCOPE}}/common-types
- @aws-starter-kit/api-client → {{PACKAGE_SCOPE}}/api-client
- "aws-starter-kit" in app.json → {{PROJECT_NAME}}
- Display name in app.json → {{PROJECT_NAME_TITLE}}
- Expo slug → {{PROJECT_NAME}}

Files to copy (preserving directory structure):
```
templates/apps/mobile/
├── package.json (mobile HAS its own package.json for Expo)
├── project.json
├── tsconfig.json
├── .eslintrc.json
├── .env.example
├── .gitignore
├── app.json (Expo config - needs token replacements)
├── babel.config.js
├── metro.config.js
├── index.js
├── jest.config.ts
├── assets/
│   └── (splash screens, icons)
└── src/
    ├── App.tsx
    ├── test-setup.ts
    ├── config/
    │   └── api.ts
    ├── store/
    │   └── user-store.ts
    └── __tests__/
        ├── App.spec.tsx
        └── store/
            └── user-store.spec.ts
```

For app.json specifically:
- "name": "{{PROJECT_NAME}}"
- "slug": "{{PROJECT_NAME}}"
- "displayName": "{{PROJECT_NAME_TITLE}}"

Do NOT copy:
- node_modules/
- .expo/
- Any .env file
  </action>
  <verify>cat packages/create-aws-starter-kit/templates/apps/mobile/app.json | grep "{{PROJECT_NAME}}"</verify>
  <done>Mobile app templates created with Expo config placeholders</done>
</task>

<task type="auto">
  <name>Task 3: Create API/infrastructure templates</name>
  <files>packages/create-aws-starter-kit/templates/apps/api/</files>
  <action>
Create templates/apps/api/ by copying apps/api/ with token replacements:

Token replacements needed:
- @aws-starter-kit/common-types → {{PACKAGE_SCOPE}}/common-types
- AwsStarterKit (CDK stack names) → {{PROJECT_NAME_PASCAL}}
- aws-starter-kit (resource names) → {{PROJECT_NAME}}
- AWS_REGION references → {{AWS_REGION}} where configurable

Files to copy (preserving directory structure):
```
templates/apps/api/
├── project.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.spec.json
├── .eslintrc.json
├── jest.config.ts
├── jest-environment.js
├── lambdas.yml
├── cdk/
│   ├── cdk.json
│   ├── tsconfig.json
│   ├── .gitignore
│   ├── app.ts (stack instantiation - needs {{PROJECT_NAME_PASCAL}})
│   ├── user-stack.ts
│   ├── static-stack.ts (needs {{PROJECT_NAME_PASCAL}} for stack naming)
│   ├── deployment-user-stack.ts
│   └── org-stack.ts
├── cdk-bootstrap/ (keep as-is - one-time setup)
└── src/
    ├── handlers/
    ├── services/
    ├── models/
    ├── schemas/
    ├── utils/
    ├── lib/
    ├── tools/ (db-export utility)
    ├── __tests__/
    └── __mocks__/
```

CDK files need careful attention:
- app.ts: Stack instantiation uses project name
- static-stack.ts: CloudFront distribution names
- user-stack.ts: Lambda function naming

Do NOT copy:
- cdk.out/ (synthesized CloudFormation)
- dist/
- node_modules/
  </action>
  <verify>grep "{{PROJECT_NAME_PASCAL}}" packages/create-aws-starter-kit/templates/apps/api/cdk/*.ts | head -5</verify>
  <done>API/CDK templates created with stack name and region placeholders</done>
</task>

<task type="auto">
  <name>Task 4: Create template manifest</name>
  <files>packages/create-aws-starter-kit/templates/manifest.json</files>
  <action>
Create manifest.json that maps all template files to their platforms:

```json
{
  "shared": [
    { "src": "root/package.json", "dest": "package.json" },
    { "src": "root/tsconfig.base.json", "dest": "tsconfig.base.json" },
    { "src": "root/nx.json", "dest": "nx.json" },
    { "src": "root/jest.preset.js", "dest": "jest.preset.js" },
    { "src": "root/eslint.config.js", "dest": "eslint.config.js" },
    { "src": "root/.npmrc", "dest": ".npmrc" },
    { "src": "root/.nvmrc", "dest": ".nvmrc" },
    { "src": "root/.editorconfig", "dest": ".editorconfig" },
    { "src": "root/.gitignore", "dest": ".gitignore" },
    { "src": "packages/common-types", "dest": "packages/common-types" },
    { "src": "packages/api-client", "dest": "packages/api-client" },
    { "src": ".vscode", "dest": ".vscode" },
    { "src": ".github", "dest": ".github" }
  ],
  "byPlatform": {
    "web": [
      { "src": "apps/web", "dest": "apps/web" }
    ],
    "mobile": [
      { "src": "apps/mobile", "dest": "apps/mobile" }
    ],
    "api": [
      { "src": "apps/api", "dest": "apps/api" }
    ]
  }
}
```

The manifest allows the generation engine (Phase 4) to:
1. Always copy shared files
2. Only copy platform directories for selected platforms
3. Know the source → destination mapping for each file/directory
  </action>
  <verify>cat packages/create-aws-starter-kit/templates/manifest.json | jq '.byPlatform | keys'</verify>
  <done>manifest.json created with shared/platform file mappings</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `nx build create-aws-starter-kit` succeeds
- [ ] templates/apps/web/ contains all web app files
- [ ] templates/apps/mobile/ contains all mobile app files
- [ ] templates/apps/api/ contains all API and CDK files
- [ ] templates/manifest.json is valid JSON with shared and byPlatform sections
- [ ] grep "{{PROJECT_NAME}}" finds matches in multiple template files
- [ ] grep "{{PROJECT_NAME_PASCAL}}" finds matches in CDK stack files
- [ ] No remaining hardcoded "aws-starter-kit" strings in templates (except intentional comments)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Platform templates properly isolated for conditional generation
- Manifest defines complete file structure for generation engine
- All placeholder tokens correctly applied
- Ready for Phase 4 (Generation Engine) to use these templates
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-system/03-03-SUMMARY.md`
</output>
