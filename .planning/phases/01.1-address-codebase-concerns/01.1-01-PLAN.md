---
phase: 01.1-address-codebase-concerns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/src/__tests__/handlers/users/update-user.spec.ts, apps/api/src/__tests__/handlers/users/delete-user.spec.ts]
autonomous: true
---

<objective>
Add missing test coverage for update-user and delete-user handlers.

Purpose: Ensure mutation operations (PUT, DELETE) are tested before templating the codebase for the CLI generator.
Output: Two new test files following existing handler test patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

@apps/api/src/__tests__/handlers/users/create-user.spec.ts
@apps/api/src/handlers/users/update-user.ts
@apps/api/src/handlers/users/delete-user.ts
@apps/api/src/services/user-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create update-user.spec.ts</name>
  <files>apps/api/src/__tests__/handlers/users/update-user.spec.ts</files>
  <action>
Create test file following the exact pattern from create-user.spec.ts:
- Import handler from '../../../handlers/users/update-user'
- Mock '../../../services/user-service'
- Test cases:
  1. Should update user successfully (200 with updated user data)
  2. Should return 400 when path parameter id is missing
  3. Should return 400 when request body is missing
  4. Should return 400 when name is invalid (empty string)
  5. Should return 404 when user not found (updateUser returns null)
  6. Should handle service errors (500)

Use createMockUserModel pattern for test data. Mock userService.updateUser for each test.
  </action>
  <verify>nx test api --testPathPattern=update-user.spec.ts passes</verify>
  <done>All 6 test cases pass, no lint errors</done>
</task>

<task type="auto">
  <name>Task 2: Create delete-user.spec.ts</name>
  <files>apps/api/src/__tests__/handlers/users/delete-user.spec.ts</files>
  <action>
Create test file following the exact pattern from create-user.spec.ts:
- Import handler from '../../../handlers/users/delete-user'
- Mock '../../../services/user-service'
- Test cases:
  1. Should delete user successfully (200 with null data, success message)
  2. Should return 400 when path parameter id is missing
  3. Should return 404 when user not found (deleteUser returns false)
  4. Should handle service errors (500)

Use jest.spyOn(userService, 'deleteUser') for mocking. deleteUser returns boolean.
  </action>
  <verify>nx test api --testPathPattern=delete-user.spec.ts passes</verify>
  <done>All 4 test cases pass, no lint errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] nx test api passes (all handler tests including new ones)
- [ ] No TypeScript errors in test files
- [ ] Both test files follow existing conventions
</verification>

<success_criteria>
- All tasks completed
- update-user.spec.ts has 6 passing tests
- delete-user.spec.ts has 4 passing tests
- Full API test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-address-codebase-concerns/01.1-01-SUMMARY.md`
</output>
