---
phase: 01-cli-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [packages/create-aws-starter-kit/src/index.ts, packages/create-aws-starter-kit/src/cli.ts]
autonomous: true
---

<objective>
Add command parsing with --help and --version flags to the CLI.

Purpose: Provide standard CLI interface patterns users expect from npm init-style tools.
Output: CLI that responds to --help (shows usage) and --version (shows package version).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-01-SUMMARY.md
@packages/create-aws-starter-kit/package.json
@packages/create-aws-starter-kit/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI module with argument parsing</name>
  <files>packages/create-aws-starter-kit/src/cli.ts</files>
  <action>
Create cli.ts that exports a `run()` function:

1. Parse process.argv using built-in Node.js (no external library needed for basic flags)
2. Check for --help or -h flag:
   - Print usage message showing: create-aws-starter-kit [options] [project-name]
   - List options: --help, --version
   - Brief description of what the tool does
   - Exit with code 0
3. Check for --version or -v flag:
   - Read version from package.json (use createRequire or dynamic import)
   - Print version number
   - Exit with code 0
4. If no flags, print welcome banner and placeholder message "Interactive wizard coming soon..."

Do NOT use external argument parsing libraries (commander, yargs). Node.js process.argv is sufficient for --help and --version. Phase 2 will add the interactive wizard with prompts library.

Use fs.readFileSync with JSON.parse to read package.json version (simpler than dynamic import for this use case). Use import.meta.url to resolve package.json path relative to the module.
  </action>
  <verify>cat packages/create-aws-starter-kit/src/cli.ts shows argument parsing logic</verify>
  <done>CLI module exists with --help and --version handling</done>
</task>

<task type="auto">
  <name>Task 2: Update entry point to use CLI module</name>
  <files>packages/create-aws-starter-kit/src/index.ts</files>
  <action>
Update index.ts to:
1. Keep the hashbang line: #!/usr/bin/env node
2. Import and call run() from ./cli.js (note .js extension for ESM)
3. Wrap in async IIFE if run() is async, or call directly if sync

Keep the entry point minimal - it should just bootstrap the CLI.
  </action>
  <verify>nx build create-aws-starter-kit succeeds</verify>
  <done>Entry point imports and calls CLI run function</done>
</task>

<task type="auto">
  <name>Task 3: Test CLI flags</name>
  <files>packages/create-aws-starter-kit/dist/index.js</files>
  <action>
Build and test the CLI:
1. Run `nx build create-aws-starter-kit`
2. Test --help: `node packages/create-aws-starter-kit/dist/index.js --help`
3. Test --version: `node packages/create-aws-starter-kit/dist/index.js --version`
4. Test default (no args): `node packages/create-aws-starter-kit/dist/index.js`

All three should work without errors. Help should show usage, version should show 0.0.1, default should show welcome message.
  </action>
  <verify>All three CLI invocations produce expected output</verify>
  <done>--help shows usage, --version shows 0.0.1, default shows welcome</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `nx build create-aws-starter-kit` succeeds
- [ ] `node packages/create-aws-starter-kit/dist/index.js --help` shows usage text
- [ ] `node packages/create-aws-starter-kit/dist/index.js --version` shows 0.0.1
- [ ] `node packages/create-aws-starter-kit/dist/index.js` shows welcome/placeholder message
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- CLI responds correctly to --help flag
- CLI responds correctly to --version flag
- CLI shows placeholder message when run without flags
- No external dependencies added (using only Node.js built-ins)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-02-SUMMARY.md`
</output>
