---
phase: 06-polish-publish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-aws-starter-kit/package.json
  - packages/create-aws-starter-kit/project.json
autonomous: true
---

<objective>
Configure the CLI package for npm publishing.

Purpose: Ensure the package is ready for `npm publish` with all required fields and files.
Output: package.json configured for npm registry, templates included in published package.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/create-aws-starter-kit/package.json
@packages/create-aws-starter-kit/project.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json for npm publish</name>
  <files>packages/create-aws-starter-kit/package.json</files>
  <action>
Update package.json with required npm publish fields:

**Fix files array** (critical - templates not currently included):
```json
"files": [
  "dist",
  "templates",
  "README.md"
]
```

**Add repository field:**
```json
"repository": {
  "type": "git",
  "url": "https://github.com/wickidcool/aws-starter-kit.git",
  "directory": "packages/create-aws-starter-kit"
}
```

**Add author field:**
```json
"author": "wickidcool"
```

**Add homepage field:**
```json
"homepage": "https://github.com/wickidcool/aws-starter-kit/tree/main/packages/create-aws-starter-kit#readme"
```

**Add bugs field:**
```json
"bugs": {
  "url": "https://github.com/wickidcool/aws-starter-kit/issues"
}
```

**Verify bin path is correct** (should be ./dist/src/index.js for Nx build output).

Do NOT change the package name - keep it as "create-aws-starter-kit" (unscoped for npx).
  </action>
  <verify>npm pack --dry-run in packages/create-aws-starter-kit shows templates/ included</verify>
  <done>package.json has all required fields and templates will be published</done>
</task>

<task type="auto">
  <name>Task 2: Add build target and prepublish verification</name>
  <files>packages/create-aws-starter-kit/project.json</files>
  <action>
Update project.json to add/verify targets:

**Verify build target exists** (should already be there from Phase 1):
```json
"build": {
  "executor": "@nx/js:tsc",
  "outputs": ["{options.outputPath}"],
  "options": {
    "outputPath": "packages/create-aws-starter-kit/dist",
    "main": "packages/create-aws-starter-kit/src/index.ts",
    "tsConfig": "packages/create-aws-starter-kit/tsconfig.json"
  }
}
```

**Add prepublish target** that runs build first:
```json
"prepublish": {
  "executor": "nx:run-commands",
  "options": {
    "commands": [
      "nx build create-aws-starter-kit",
      "cd packages/create-aws-starter-kit && npm pack --dry-run"
    ],
    "parallel": false
  }
}
```

This ensures build is fresh and pack preview shows correct files before any publish.
  </action>
  <verify>nx prepublish create-aws-starter-kit runs successfully and shows templates in pack output</verify>
  <done>Build and prepublish targets work correctly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm pack --dry-run` in CLI package shows templates/ directory
- [ ] `npm pack --dry-run` shows README.md
- [ ] `npm pack --dry-run` shows dist/ directory
- [ ] package.json has repository, author, homepage, bugs fields
- [ ] `nx prepublish create-aws-starter-kit` runs without errors
</verification>

<success_criteria>

- All tasks completed
- templates/ will be included in npm package
- All required npm metadata fields present
- Package ready for `npm publish`
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-publish/06-03-SUMMARY.md`
</output>
