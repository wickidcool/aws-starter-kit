---
phase: 06-polish-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/create-aws-starter-kit/jest.config.ts
  - packages/create-aws-starter-kit/src/__tests__/validation/project-name.spec.ts
  - packages/create-aws-starter-kit/src/__tests__/prompts/project-name.spec.ts
  - packages/create-aws-starter-kit/src/__tests__/wizard.spec.ts
  - packages/create-aws-starter-kit/src/__tests__/generator/replace-tokens.spec.ts
autonomous: true
---

<objective>
Add unit test coverage for the CLI package.

Purpose: Ensure CLI validation, prompts, wizard, and generator logic work correctly before npm publish.
Output: Jest test suite with coverage for core CLI modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md

@packages/create-aws-starter-kit/package.json
@packages/create-aws-starter-kit/src/validation/project-name.ts
@packages/create-aws-starter-kit/src/prompts/project-name.ts
@packages/create-aws-starter-kit/src/wizard.ts
@packages/create-aws-starter-kit/src/generator/replace-tokens.ts
@packages/create-aws-starter-kit/src/templates/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Jest for CLI package</name>
  <files>packages/create-aws-starter-kit/jest.config.ts, packages/create-aws-starter-kit/project.json</files>
  <action>
Create jest.config.ts for CLI package following monorepo patterns:
- Use @nx/jest/preset
- Set testEnvironment: 'node'
- Configure ts-jest with ESM support (useESM: true)
- Set extensionsToTreatAsEsm: ['.ts']
- Add moduleNameMapper for .js extensions (ESM imports)
- Set transform to use ts-jest with isolatedModules

Add "test" target to project.json using @nx/jest:jest executor.

Do NOT add test script to package.json - use Nx target instead.
  </action>
  <verify>nx test create-aws-starter-kit runs (may show 0 tests initially)</verify>
  <done>Jest configured and Nx test target works</done>
</task>

<task type="auto">
  <name>Task 2: Write validation and token tests</name>
  <files>packages/create-aws-starter-kit/src/__tests__/validation/project-name.spec.ts, packages/create-aws-starter-kit/src/__tests__/generator/replace-tokens.spec.ts</files>
  <action>
Create test files following existing monorepo patterns:

**project-name.spec.ts:**
- Test validateProjectName function
- Cases: valid names ('my-app', 'app123'), invalid names ('My App', '123app', '@scope/pkg', empty string)
- Import from '../../validation/project-name.js' (ESM extension)

**replace-tokens.spec.ts:**
- Test replaceTokens function from generator
- Cases: single token replacement, multiple tokens, unknown token preserved, no tokens (passthrough)
- Use TOKENS constant from templates/tokens

Use describe/it/expect pattern from @jest/globals.
  </action>
  <verify>nx test create-aws-starter-kit --testPathPattern=validation passes; nx test create-aws-starter-kit --testPathPattern=replace-tokens passes</verify>
  <done>Validation and token replacement have test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Write wizard integration test</name>
  <files>packages/create-aws-starter-kit/src/__tests__/wizard.spec.ts</files>
  <action>
Create wizard test that mocks the prompts library:

- Mock 'prompts' module to return controlled responses
- Test runWizard returns complete ProjectConfig when all prompts answered
- Test runWizard returns null when user cancels (onCancel triggered)
- Verify returned config has all expected fields (projectName, platforms, features, awsRegion, brandColor)

Use jest.mock('prompts') at top of file.
Use jest.mocked() for type-safe mock access.
  </action>
  <verify>nx test create-aws-starter-kit --testPathPattern=wizard passes</verify>
  <done>Wizard orchestration has test coverage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `nx test create-aws-starter-kit` runs all tests
- [ ] All tests pass (validation, replace-tokens, wizard)
- [ ] No TypeScript errors in test files
</verification>

<success_criteria>

- All tasks completed
- Jest configured for CLI package with ESM support
- Core modules have unit test coverage
- All tests pass via `nx test create-aws-starter-kit`
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-publish/06-01-SUMMARY.md`
</output>
