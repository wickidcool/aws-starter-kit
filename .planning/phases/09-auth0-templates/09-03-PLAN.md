---
phase: 09-auth0-templates
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - packages/create-aws-starter-kit/src/templates/manifest.ts
  - packages/create-aws-starter-kit/templates/apps/web/.env.example
  - packages/create-aws-starter-kit/src/__tests__/generator.spec.ts
autonomous: true
---

<objective>
Wire Auth0 templates into the generator with manifest entries, environment variables, and test coverage.

Purpose: Enable the generator to copy Auth0-specific templates when auth0 provider is selected.
Output: Updated manifest with byAuthProvider entries for auth0, .env.example with Auth0 variables, generator tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context - patterns to follow:
@.planning/phases/08-cognito-templates/08-04-SUMMARY.md

# Source files to modify:
@packages/create-aws-starter-kit/src/templates/manifest.ts
@packages/create-aws-starter-kit/templates/apps/web/.env.example
@packages/create-aws-starter-kit/src/__tests__/generator.spec.ts

# Prior plan outputs to integrate:
# 09-01-SUMMARY.md and 09-02-SUMMARY.md will be available after Wave 1 completes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest with Auth0 byAuthProvider entries</name>
  <files>packages/create-aws-starter-kit/src/templates/manifest.ts</files>
  <action>
Update templateManifest.byAuthProvider.auth0 array with entries for Auth0 templates.

Add these entries to the auth0 array (currently empty []):
```typescript
auth0: [
  { src: 'apps/web/src/auth/auth0-provider.tsx', dest: 'apps/web/src/auth/auth0-provider.tsx' },
  { src: 'apps/web/src/config/auth0-config.ts', dest: 'apps/web/src/config/auth0-config.ts' },
  { src: 'apps/api/src/middleware/auth0-auth.ts', dest: 'apps/api/src/middleware/auth0-auth.ts' },
],
```

Note: The shared auth files (auth.types.ts, use-auth.ts, auth-context.ts) are already copied by the platform entries or shared section. The byAuthProvider only adds the provider-specific files.
  </action>
  <verify>grep "auth0-provider.tsx" src/templates/manifest.ts confirms entry present</verify>
  <done>byAuthProvider.auth0 array contains all Auth0 template entries</done>
</task>

<task type="auto">
  <name>Task 2: Add Auth0 environment variables to .env.example</name>
  <files>packages/create-aws-starter-kit/templates/apps/web/.env.example</files>
  <action>
Update .env.example to include Auth0 environment variables alongside the existing Cognito variables.

Add a section for Auth0:
```
# Auth0 Configuration (if using Auth0)
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id
VITE_AUTH0_AUDIENCE=https://your-api-identifier
```

This should be added after the Cognito section. Both sections exist in the template, and the generator doesn't strip based on provider selection (that would require more complex logic). Users use whichever section applies to their provider choice.
  </action>
  <verify>grep "AUTH0_DOMAIN" .env.example confirms variables present</verify>
  <done>.env.example contains VITE_AUTH0_DOMAIN, VITE_AUTH0_CLIENT_ID, VITE_AUTH0_AUDIENCE</done>
</task>

<task type="auto">
  <name>Task 3: Add generator tests for Auth0 provider</name>
  <files>packages/create-aws-starter-kit/src/__tests__/generator.spec.ts</files>
  <action>
Add test cases for Auth0 provider in the generator test suite.

Add tests:
1. "should derive correct tokens for Auth0 provider" - verify AUTH_AUTH0='true', AUTH_COGNITO='false' when provider is 'auth0'
2. "should include auth0 entries in manifest when provider is auth0" - verify byAuthProvider.auth0 has entries

Follow the pattern of existing tests for Cognito in this file.

Example test structure:
```typescript
it('should derive AUTH_AUTH0 token correctly', () => {
  const config: ProjectConfig = {
    projectName: 'test-app',
    platforms: ['web', 'api'],
    awsRegion: 'us-east-1',
    features: [],
    brandColor: 'blue',
    auth: { provider: 'auth0', features: [] }
  };
  const tokens = deriveTokenValues(config);
  expect(tokens.AUTH_AUTH0).toBe('true');
  expect(tokens.AUTH_COGNITO).toBe('false');
});
```
  </action>
  <verify>nx test create-aws-starter-kit passes with new tests</verify>
  <done>Generator tests cover Auth0 token derivation and manifest entries</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] manifest.ts byAuthProvider.auth0 has 3 entries
- [ ] .env.example has Auth0 environment variables
- [ ] generator.spec.ts has Auth0 test cases
- [ ] nx test create-aws-starter-kit passes
- [ ] tsc --noEmit passes
</verification>

<success_criteria>

- All tasks completed
- byAuthProvider.auth0 contains auth0-provider.tsx, auth0-config.ts, auth0-auth.ts entries
- .env.example includes Auth0 configuration variables
- Generator tests verify Auth0 token derivation
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-auth0-templates/09-03-SUMMARY.md`
</output>
